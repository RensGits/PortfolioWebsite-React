
// in ProjectTile --> button

const [checked, setChecked] = useState(false);

<button onClick= {
    () => setChecked((value) => !value, console.log(checked))
    } >
</button>


in home --> projectTile component : 

if checked(true) {
  setExpand (grid-column-template: 
}


react three fiber setup: https://levelup.gitconnected.com/rendering-3d-objects-and-animation-in-react-using-three-fiber-bf0255e642be
