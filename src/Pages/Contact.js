import { motion } from 'framer-motion';
import React from 'react';

function Contact () {
    return (
        <motion.div>
        <p>Contact Page</p>
        </motion.div>

    );
}

export default Contact;